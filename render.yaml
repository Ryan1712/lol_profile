services:
  - type: web
    name: lol-scouting-tool
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: RIOT_API_KEY
        sync: false
      - key: REGION
        value: vn2
